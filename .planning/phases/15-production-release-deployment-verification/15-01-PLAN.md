---
phase: 15-production-release-deployment-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_breakdown_submission_and_linking.mjs
  - supabase/tests/test_log_sequencing_and_overlap.mjs
  - supabase/tests/test_operator_machine_assignments.mjs
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "All 6 automated database regression test suites in supabase/tests/*.mjs execute against the live database with 100% pass rate."
    - "test_breakdown_submission_and_linking.mjs exercises idempotency key uniqueness without premature failure from future shift checks."
    - "test_log_sequencing_and_overlap.mjs loads root .env credentials and successfully verifies machine log sequencing and overlap prevention."
    - "test_operator_machine_assignments.mjs successfully asserts capacity limits, GiST shift exclusion conflicts, and atomic assignment closure using valid end_reason enums."
    - "verify_seed.mjs executes cleanly confirming relational integrity across database tables."
  artifacts:
    - supabase/tests/test_breakdown_submission_and_linking.mjs
    - supabase/tests/test_log_sequencing_and_overlap.mjs
    - supabase/tests/test_operator_machine_assignments.mjs
  key_links:
    - "test_breakdown_submission_and_linking.mjs passes past dates (2024-01-02) to satisfy migration 055 chk_shift_end_not_in_future"
    - "test_log_sequencing_and_overlap.mjs loads ../../.env and reads SUPABASE_SECRET_KEY"
    - "test_operator_machine_assignments.mjs checks res.code and uses end_reason 'removed'"
---

<objective>
Stabilize and execute all 6 automated database integration test suites in `supabase/tests/*.mjs` and the database seed verification script `supabase/verify_seed.mjs`, ensuring 100% pass rate with zero errors across all test suites as required by Phase 15.

Purpose: Guarantee database schema constraints, RLS policies, triggers, and atomic RPCs are fully verified against the live Supabase instance prior to production release.
Output: Fixed test suites in `supabase/tests/` and a verified 100% passing test execution report.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-production-release-deployment-verification/15-RESEARCH.md
@supabase/tests/test_future_shift_validation.mjs
@supabase/tests/test_shift_timing_and_lunch_inclusion.mjs
@supabase/tests/test_operator_complete_matrix.mjs
@supabase/tests/test_breakdown_submission_and_linking.mjs
@supabase/tests/test_log_sequencing_and_overlap.mjs
@supabase/tests/test_operator_machine_assignments.mjs
@supabase/verify_seed.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize Test Suites and Align Assertions with Database Schema</name>
  <files>
    supabase/tests/test_breakdown_submission_and_linking.mjs,
    supabase/tests/test_log_sequencing_and_overlap.mjs,
    supabase/tests/test_operator_machine_assignments.mjs
  </files>
  <read_first>
    supabase/migrations/047_operator_machine_assignments_and_shift_overtime.sql,
    supabase/migrations/055_fix_date_validation_trigger_time_comparison.sql,
    supabase/tests/test_operator_complete_matrix.mjs
  </read_first>
  <action>
    Fix the three test scripts that have schema or environment discrepancies:

    1. `supabase/tests/test_breakdown_submission_and_linking.mjs`:
       - In Test 3b and 3c (idempotency key tests around line 170-190), replace future dates `log_date: "2029-01-02"` and `end_datetime: "2029-01-02T14:00:00+05:30"` with valid past dates: `log_date: "2024-01-02"`, `start_datetime: "2024-01-02T08:00:00+05:30"`, `end_datetime: "2024-01-02T14:00:00+05:30"`. This prevents migration 055's `chk_shift_end_not_in_future` (code 23514) from firing prematurely before the unique `idempotency_key` constraint (code 23505) is exercised.

    2. `supabase/tests/test_log_sequencing_and_overlap.mjs`:
       - Update the `loadEnv` section to load root `../../.env`:
         `loadEnv(path.resolve(__dirname, "../../.env"));`
       - Update the Supabase service key fallback to check `process.env.SUPABASE_SECRET_KEY`:
         `const supabaseKey = process.env.SUPABASE_SECRET_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY;`
       - Ensure that if `!supabaseKey`, it logs a clean error rather than silently defaulting to an expired/unprivileged `anon` key.

    3. `supabase/tests/test_operator_machine_assignments.mjs`:
       - In Test 4 (line 237): The atomic RPC `assign_operator_machine_atomic` returns `{ success: false, code: 'MAX_OPERATORS_REACHED', error: '...' }`. Update the assertion check to:
         `const isCapacityBlocked = ass4?.code === 'MAX_OPERATORS_REACHED' || ass4Err?.message?.includes('MAX_OPERATORS_REACHED');`
       - In Test 5 (line 252): The atomic RPC catches PostgreSQL `23P01` and returns `{ success: false, code: 'SHIFT_OVERLAP_CONFLICT', error: '...' }`. Update the assertion check to:
         `const isExclusionBlocked = assExcl?.code === 'SHIFT_OVERLAP_CONFLICT' || assExclErr?.code === '23P01';`
       - In Test 6 and cleanups (lines 173, 261, 284): Migration 047 enforces `end_reason IN ('reassigned', 'removed', 'shift_changed', 'migrated')`. Replace `'test_reset'`, `'completed'`, and `'test_cleanup'` with `'removed'`.
  </action>
  <verify>
    node supabase/tests/test_breakdown_submission_and_linking.mjs && node supabase/tests/test_log_sequencing_and_overlap.mjs && node supabase/tests/test_operator_machine_assignments.mjs
  </verify>
  <acceptance_criteria>
    - test_breakdown_submission_and_linking.mjs exits with code 0 and reports all tests passed
    - test_log_sequencing_and_overlap.mjs exits with code 0 and finds seeded machines
    - test_operator_machine_assignments.mjs exits with code 0 and reports 13 PASSED | 0 FAILED
  </acceptance_criteria>
  <done>All 3 updated test scripts execute cleanly against the live Supabase database with 0 assertion failures.</done>
</task>

<task type="auto">
  <name>Task 2: Full Regression Test Suite Execution & Seed Verification</name>
  <files>
    supabase/verify_seed.mjs
  </files>
  <read_first>
    supabase/verify_seed.mjs
  </read_first>
  <action>
    Execute the entire automated verification pipeline across all 6 test suites and verify seed integrity:
    
    1. Run `node supabase/tests/test_future_shift_validation.mjs`
    2. Run `node supabase/tests/test_shift_timing_and_lunch_inclusion.mjs`
    3. Run `node supabase/tests/test_operator_complete_matrix.mjs`
    4. Run `node supabase/tests/test_breakdown_submission_and_linking.mjs`
    5. Run `node supabase/tests/test_log_sequencing_and_overlap.mjs`
    6. Run `node supabase/tests/test_operator_machine_assignments.mjs`
    7. Run `node supabase/verify_seed.mjs`

    Verify that:
    - Every suite outputs green PASS indicators.
    - Zero failures or unhandled promise rejections occur.
    - Total passed assertions exceed 140 across all suites.
    - Seed tables confirm row counts for `users`, `machines`, `machine_hour_logs`, and `audit_logs`.
  </action>
  <verify>
    node -e "
      import { execSync } from 'child_process';
      const suites = [
        'supabase/tests/test_future_shift_validation.mjs',
        'supabase/tests/test_shift_timing_and_lunch_inclusion.mjs',
        'supabase/tests/test_operator_complete_matrix.mjs',
        'supabase/tests/test_breakdown_submission_and_linking.mjs',
        'supabase/tests/test_log_sequencing_and_overlap.mjs',
        'supabase/tests/test_operator_machine_assignments.mjs',
        'supabase/verify_seed.mjs'
      ];
      for (const s of suites) {
        console.log('Running: ' + s);
        execSync('node ' + s, { stdio: 'inherit' });
      }
      console.log('ALL 6 SUITES + SEED VERIFIED WITH 0 ERRORS');
    "
  </verify>
  <acceptance_criteria>
    - All 6 test suites execute sequentially with exit code 0
    - Output concludes with "ALL 6 SUITES + SEED VERIFIED WITH 0 ERRORS"
    - Database state is verified without orphaned test records
  </acceptance_criteria>
  <done>Zero errors across all test suites (supabase/tests/*.mjs) and complete database verification achieved.</done>
</task>

</tasks>
